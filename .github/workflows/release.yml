name: Release to PyPI

# 推送形如 v0.3.0 的标签即触发构建并发布
on:
  push:
    tags:
      - "v*"

jobs:
  # 1) 校验标签号与 setup.py 中的版本一致，避免发错版本
  check-version:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify tag matches setup.py version
        run: |
          TAG="${GITHUB_REF_NAME#v}"
          VER="$(grep -oP 'version=\"\K[^\"]+' setup.py | head -1)"
          echo "tag=$TAG  setup.py=$VER"
          if [ "$TAG" != "$VER" ]; then
            echo "::error::标签 $GITHUB_REF_NAME 与 setup.py 版本 $VER 不一致"
            exit 1
          fi

  # 2) 在 Windows 上用 cibuildwheel 构建各 Python 版本的轮子
  build-wheels:
    needs: check-version
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build wheels
        uses: pypa/cibuildwheel@v4.0.0   # 如需更新版本，改这里
      - uses: actions/upload-artifact@v4
        with:
          name: cibw-wheels
          path: ./wheelhouse/*.whl

  # 3) 构建源码分发包（sdist）
  build-sdist:
    needs: check-version
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.x"
      - name: Build sdist
        run: |
          python -m pip install --upgrade build
          python -m build --sdist
      - uses: actions/upload-artifact@v4
        with:
          name: cibw-sdist
          path: dist/*.tar.gz

  # 4) 发布到 PyPI（使用 Trusted Publishing / OIDC，无需 API Token）
  publish:
    needs: [build-wheels, build-sdist]
    runs-on: ubuntu-latest
    environment:
      name: pypi
      url: https://pypi.org/project/srbpy/
    permissions:
      id-token: write          # Trusted Publishing 必需
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: dist
          pattern: cibw-*
          merge-multiple: true
      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
